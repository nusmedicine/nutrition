{
  "name": "llm-patient-spike",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Throwaway bake-off: simulated patient via local WebLLM (WebGPU) vs hosted Qwen. NOT part of the book build.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "eval:hosted": "node eval/run-hosted.mjs"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.84"
  },
  "devDependencies": {
    "vite": "^8.1.2"
  }
}
